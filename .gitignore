# Python
.venv/
__pycache__/
*.pyc
backend/.env
.env
frontend/.env.local
.env.local

# Local data (encounter ledger, etc.)
backend/data/

# Per-user launcher markers (created by install.bat / start.bat)
.scout/

# Release build output + the manifest the updater writes into installed trees
dist/
/release-manifest.json

# Not shipped: running install.bat again repairs any issue diagnose.bat reported
/diagnose.bat

# Local-only personal offline launcher (never ship to any repo)
/launch-offline.bat
/_run_offline.py

# Node / Next
frontend/node_modules/
frontend/.next/
frontend/out/
npm-debug.log*

# .NET / WPF (native desktop frontend)
wpf/bin/
wpf/obj/
wpf/publish/
.vs/
*.user
*.suo

# Reference clones (logic already integrated into backend/)
vendor/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

ops/

# Claude Code local config
.claude/
node_modules_bak/
