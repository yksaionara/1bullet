# Pinned to the exact versions this release was built and tested against, so a
# fresh install can never pull a newer, untested package. Regenerate with:
#   .venv/Scripts/python -m pip freeze > backend/requirements.txt
# (then re-add these comments), and re-test install.bat on a clean machine.

# --- direct dependencies ---
Flask==3.1.3
flask-cors==6.0.3
requests==2.34.2
python-dotenv==1.2.2
urllib3==2.7.0
rich==15.0.0
pypresence==4.6.1      # Discord Rich Presence (optional; degrades if Discord/lib absent)
websockets==13.1       # local WebSocket bridge for the local dashboard
valclient==2.28        # local Riot client API (career/match-history endpoints)
cryptography==49.0.0   # offline-mode: convert Deceive's PKCS#12 chat cert -> PEM

# --- transitive dependencies ---
# pip installs these anyway; pinned so the resolver can't swap in a new version
# underneath us. This file doubles as the constraints file for run.py's
# on-demand installs, so every version lives here and nowhere else.
anyio==4.13.0
blinker==1.9.0
certifi==2026.5.20
cffi==2.0.0            # via cryptography
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
pycparser==2.22        # via cffi
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
httpx==0.28.1
hyperframe==6.1.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
methoddispatch==3.0.2
msgpack==1.1.2
pyee==12.1.1
Pygments==2.20.0
typing_extensions==4.15.0
Werkzeug==3.1.8
